pipeline {
        agent any 
        stages {
        stage('Git Fetch') {
            steps {
                echo 'this is git fetch stage '
           }
        }

        stage('Git Checkout') {
            steps {
               echo 'this is git fetch stage '
            }
        }

         stage('Git Pull') {
            steps {
                echo 'this is git fetch stage '
            }
        }
       
         stage('Install Composer') {
            steps {
                echo 'this is git fetch stage '
            }
        }
            stage('Optimise Clear') {
            steps {
               echo 'this is git fetch stage '
            }
        }  
    }
}
